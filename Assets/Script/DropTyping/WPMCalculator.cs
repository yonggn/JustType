using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WPMCalculator : MonoBehaviour
{

    /*public int wpm;

    public int wordSuccess;

    public float timer;

    public int totalWPM;

    public int aveWPM;


    // Update is called once per frame
    void Update()
    {
        if(Word.startTimer&&!WordToDisplay.wordComplete)
        {
            timer += Time.deltaTime % 60;
        }
        if(WordToDisplay.wordComplete&&Word.wordTyped)
        {
            CalculateTheWPM();

        }
  
    }

    public void CalculateTheWPM()
    {
        wordSuccess++;
        Debug.Log("Timer: " + timer);
        wpm = (Word.letterCount / (int)timer)*60;
        totalWPM += wpm;
        //PlayerPrefs.SetInt("WPM", totalWPM / wordSuccess);  //average wpm
        //Debug.Log(PlayerPrefs.GetInt("WPM") + " wpm");
        //Problem here

        aveWPM = totalWPM / wordSuccess;

        Debug.Log("Averange WPM: " + aveWPM);
        Debug.Log("Total WPM: " + totalWPM);
        timer = 0;

    }*/


}
